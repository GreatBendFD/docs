// Fill these in from your Supabase project (Project Settings, then API Keys).
// The URL and the PUBLISHABLE key (older projects call it the "anon" key) are designed to be public.
// NEVER put the secret key (also called the service role key) here or anywhere in this repository.
window.GBFD_CONFIG = {
  SUPABASE_URL: 'https://YOUR-PROJECT-ID.supabase.co',
  SUPABASE_KEY: 'YOUR-PUBLISHABLE-KEY'
};
